<Window x:Class="Project.View.BucketPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project.View"
        mc:Ignorable="d"
        Title="BucketPage" Height="450" Width="800">
    <Grid>
        <DataGrid Grid.Row="0" AutoGenerateColumns="False" Margin="0,0,0,55" ItemsSource="{Binding buckets}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Количество"></DataGridTextColumn>
                <DataGridTextColumn Header="Цена"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Label Margin="0,379,496,16">Общая цена:</Label>
    </Grid>
</Window>
